{
  "name": "day-13",
  "version": "0.0.0",
  "description": "Today's task will be to simply generate a new site using the Gatsby CLI and deploy it to my favorite static and [JAMstack](https://jamstack.org) host - [ZEIT Now](https://zeit.co).",
  "author": "Rob Brennan <rob@therobbrennan.com> (therobbrennan.com)",
  "license": "ISC",
  "scripts": {
    "start": "docker-compose up",
    "build": "docker-compose up --remove-orphans --build --force-recreate",
    "stop": "docker-compose down",
    "docker:nuke": "docker system prune -a && docker volume prune",
    "deploy": "cd app && npm run deploy"
  }
}
